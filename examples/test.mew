// A simple test file for the mew language
catst name = "Whiskers";
purr("Hello, " + name + "!");

meow? (3 > 2) {
    purr("Math works!");
}

// Function definition
cat sayMeow(times) {
    fur (catlt i = 0; i < times; i = i + 1) {
        purr("Meow!");
    }
    return "All done meowing!";
}

// Call the function
sayMeow(3); 